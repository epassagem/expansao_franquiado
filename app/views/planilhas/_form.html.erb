<%= simple_form_for(@planilha) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :estatus %>
    <%= f.input :interessado_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
